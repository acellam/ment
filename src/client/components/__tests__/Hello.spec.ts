import { expect } from "chai";
import "mocha";

describe("Metvn", () => {

    describe("Should do this one", () => {
        it("1 to be 1", () => {
            // test one
        });

        it("should return hello world", () => {
            expect(1).to.equal(1);
        });
    });
});
